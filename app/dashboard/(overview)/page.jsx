export default async function Page() {
  return (
    <main>
      <div>
        <h1>Dashboard</h1>
      </div>
      <div>
        <h2>List of Decisions</h2>
        {/* Only add upto 5 decisions taken from the database
        Add navigation buttons to be taken to FORM, HOME(DASHBOARD), DECISIONS and CREATEUSER
        */}
        {/* <Table></Table> */}
      </div>
    </main>
  );
}
